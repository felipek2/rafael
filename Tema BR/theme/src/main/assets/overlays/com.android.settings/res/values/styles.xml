<?xml version="1.0" encoding="utf-8"?>
<resources xmlns="http://schemas.android.com/apk/res/android">

    <style name="Theme.SettingsBase" parent="@android:style/Theme.Material.Settings">
        <item name="android:colorAccent">@color/theme_accent</item>
        <item name="android:colorControlActivated">@color/theme_accent</item>
    </style>

    <style name="Theme.ActionBar" parent="@android:style/Widget.Material.Light.ActionBar.Solid">
        <item name="android:elevation">0dp</item>
    </style>

    <!-- define o estilo do texto da descrição da categoria -->
    <style name="TextAppearance.CategoryTitle" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/text_categoria</item>
    </style>

</resources>